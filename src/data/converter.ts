import * as R from 'ramda';
import { categoryConfigList, unitList } from './unitData';
import { UnitId, Unit, ConverterItem } from '../types';

function converter(fromItems: ConverterItem[], toIds: UnitId[]) {}
